#pragma once

#include "src/Common/Common.h"
#include "Pass/RenderPass.h"
#include <memory>

namespace Xunlan
{
    /*class RenderResource;

    class RenderPassManager final
    {
    public:

        void Initialize();
        void Shutdown();

        void RenderForwardPlus(const RenderResource& renderResource);

    private:

        std::unique_ptr<RenderPass> m_gPass;
        std::unique_ptr<RenderPass> m_postProcessPass;
    };*/
}